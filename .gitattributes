# 全部文本一律以 LF 入库并检出。
# 在 Windows 上编辑时若被转成 CRLF，Linux 上会因 shebang 变成 "bash\r"
# 而报 "bad interpreter: No such file or directory"。
* text=auto eol=lf

*.sh  text eol=lf
*.yml text eol=lf
*.md  text eol=lf
